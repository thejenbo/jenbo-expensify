import React from 'react';
import HeaderNav from './HeaderNav';

const AppHeader = ({ startLogOut }) => (
    <header>
        <h1>expen$ify</h1>
        <HeaderNav />
    </header>
);

export default AppHeader;
